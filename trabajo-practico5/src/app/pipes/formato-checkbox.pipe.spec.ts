import { FormatoCheckboxPipe } from './formato-checkbox.pipe';

describe('FormatoCheckboxPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoCheckboxPipe();
    expect(pipe).toBeTruthy();
  });
});
